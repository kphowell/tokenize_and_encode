This is a test delpoyment for the LivePerson Algorithmia POC.

This repository includes two models: a tokenizer and an encoder.
The expected input is a list of messages. 
The output is an array of embeddings.